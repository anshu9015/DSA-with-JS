// Easy Level (1–20)
// Reverse a string                                                                   done

// Check if a string is a palindrome                                                    done

// Count vowels and consonants in a string                                                   done

// Check if two strings are anagrams                                                           done

// Remove duplicate characters from a string                                                      done

// Find the first non-repeating character                                                            done

// Check if two strings are rotations of each other                                                 not in optimized way but done

// Count the number of words in a string                                                             done

// Capitalize the first letter of each word

// Convert a string to a character array                                                            done

// Count the frequency of each character in a string                                                  done

// Replace all spaces in a string with %20

// Check if a string contains only digits

// Remove all occurrences of a given character from string

// Toggle case of each character in a string

// Check if a string has all unique characters                                                            done

// Print all substrings of a string                                                                         done

// Find the length of the shortest and longest word in a string

// Count number of uppercase and lowercase letters

// Find the most frequent character in a string                                                              done

// ⚙️ Medium Level (21–40)
// Group anagrams from an array of strings

// Find the longest common prefix among strings

// Find the longest palindromic substring

// Check if one string is a subsequence of another

// Perform basic string compression (e.g., aabcc → a2b1c2)

// Find the longest substring without repeating characters

// Implement strStr() (return index of substring)

// Count the number of palindromic substrings

// Find the first repeated character in a string

// Check if two strings are one edit away

// Check if a string is a permutation of a palindrome

// Find the minimum window substring that contains all characters of another string

// Check if all characters in a string appear the same number of times

// Sort characters in a string by frequency

// Remove all adjacent duplicates in string

// Count and say sequence (e.g., 1, 11, 21, 1211...)

// Find the longest even-length substring such that the sum of first and second half is same

// ZigZag Conversion (Leetcode #6)

// Reorganize a string so that no two adjacent characters are the same

// Decode a string (e.g., 3[a]2[bc] → aaabcbc)
